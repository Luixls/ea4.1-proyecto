<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>Eventos de la Sección</title>
    <style>
        body { font-family: Arial, sans-serif; background-color: #f5f5f5; margin: 0; padding: 20px; }
        h1 { color: #333; text-align: center; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th, td { text-align: left; padding: 8px; border-bottom: 1px solid #ddd; }
        th { background-color: #4caf50; color: white; }
        tr:hover { background-color: #f5f5f5; }
        td { color: #333; }
    </style>
  </head>
  <body>
    <% if(seccionInfo) { %>
    <h1>Eventos de la Sección: <%= seccionInfo.seccion_nombre %></h1>
    <h2>Profesor: <%= seccionInfo.profesor_nombre %></h2>
    <h2>Materia: <%= seccionInfo.materia_nombre %></h2>
    <h2>Trimestre: <%= seccionInfo.trimestre_nombre %></h2>
    <% } else { %>
    <p>No se encontraron detalles para la sección especificada.</p>
    <% } %> <% if(eventos && eventos.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>Evento</th>
          <th>Número de Semana</th>
          <th>Fecha</th>
          <th>Rasgos</th>
          <th>Global</th>
        </tr>
      </thead>
      <tbody>
        <% eventos.forEach(evento => { %>
        <tr>
          <td><%= evento.evento_nombre %></td>
          <td><%= evento.numero_semana %></td>
          <td><%= evento.fecha %></td>
          <td><%= evento.rasgos %></td>
          <td><%= evento.es_global ? 'Sí' : 'No' %></td>
        </tr>
        <% }) %>
      </tbody>
    </table>
    <% } else { %>
    <p>No hay eventos para mostrar.</p>
    <% } %>
  </body>
</html>
